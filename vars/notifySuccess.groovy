// Notify of a successful result
def call(
    String title,
    String message,
    String channels
) {
    notifyChannels (
        title,
        message,
        channels,
        "#50C878"
    )
}

