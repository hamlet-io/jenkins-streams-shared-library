// Notify of an unsuccessful result
def call(
    String title,
    String message,
    String channels
) {
    notifyChannels (
        title,
        message,
        channels,
        "#B22222"
    )
}
